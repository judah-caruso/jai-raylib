# Raylib

Raylib bindings for the Jai language. This uses Jai's builtin math types instead of defining its own.

# Note

These are largely unfinished due to me only binding what I need at a given time.
I'll get around to binding everything soon.

# Usage

See `examples/*.jai` for usage.
